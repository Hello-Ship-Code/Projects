<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>short hand url</title>
   <style>/* Global styles */
    body {
        font-family: Arial, sans-serif;
        background-color: #121212;  /* Dark background */
        color: #f5f5f5; /* Light text */
        text-align: center;
        margin: 0;
        padding: 20px;
    }
    
    /* Heading */
    h1 {
        font-size: 2rem;
        color: #ffffff; /* White text */
    }
    
    /* Form Container */
    div {
        background: #1e1e1e; /* Dark gray */
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 4px 10px rgba(255, 255, 255, 0.1);
        max-width: 400px;
        margin: 20px auto;
    }
    
    /* Form Elements */
    form {
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    
    label {
        font-size: 1.1rem;
        margin-bottom: 8px;
    }
    
    input {
        width: 90%;
        padding: 10px;
        border: 2px solid #444;
        background: #222;
        color: white;
        border-radius: 5px;
        font-size: 1rem;
        margin-bottom: 10px;
        transition: border-color 0.3s;
    }
    
    input:focus {
        border-color: #007bff;
        outline: none;
    }
    
    button {
        background: #007bff;
        color: white;
        font-size: 1rem;
        padding: 10px 15px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background 0.3s;
    }
    
    button:hover {
        background: #0056b3;
    }
    
    /* Table Styling */
    table {
        width: 80%;
        margin: 20px auto;
        border-collapse: collapse;
        background: #1e1e1e;
        box-shadow: 0 4px 10px rgba(255, 255, 255, 0.1);
        border-radius: 8px;
        overflow: hidden;
    }
    
    th, td {
        padding: 12px;
        text-align: center;
        border-bottom: 1px solid #444;
    }
    
    th {
        background: #007bff;
        color: white;
    }
    
    tr:hover {
        background: #333;
    }
    
    td a {
        text-decoration: none;
        color: #00bfff;
        font-weight: bold;
        transition: color 0.3s;
    }
    
    td a:hover {
        color: #ffcc00;
    }
    </style>
</head>
<body>
    <h1>Short hand url</h1>
   <div>
    <form method="POST" action="/url" enctype="application/x-www-form-urlencoded">
        <label for="redirectUrl">Enter your URL</label>
        <input type="text" id="redirectUrl" name="redirectUrl" placeholder="https://example.com" required>
        <button type="submit">Add me</button>
    </form>
   </div>
   <table>
    <% if (redirectUrl && redirectUrl.length > 0) { %>
        <tr>
            <th>Short ID</th>
            <th>Redirect URL</th>
            <th>Number of Visits</th>
        </tr>
        <% redirectUrl.forEach(element => { %>
            <% if (element) { %>
            <tr>
                <td>
                    <a href="/url/<%= element.shortID %>"><%= element.shortID %></a>
                </td>
                <td>
                    <a href="<%= element.redirectUrl %>"><%= element.redirectUrl %></a>
                </td>
                <td><%= element.visitHistory ? element.visitHistory.length : 0 %></td>
            </tr>
            <% } %>
        <% }); %>
    <% } else { %>
        <p>No URLs found</p>
    <% } %>
</table>


</body>
</html>
